<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/dp_8"
    android:layout_marginBottom="@dimen/dp_10"
    android:background="@android:color/transparent"
    app:cardCornerRadius="@dimen/dp_6"
    app:layout_constraintTop_toTopOf="parent">

    <FrameLayout
        android:id="@+id/item_attach_photo__iv_wrapper"
        android:layout_width="@dimen/dp_100"
        android:layout_height="@dimen/dp_100">

        <ImageView
            android:id="@+id/item_attach_photo__iv"
            android:layout_width="@dimen/dp_100"
            android:layout_height="@dimen/dp_100"
            android:scaleType="centerCrop" />

        <ImageView
            android:id="@+id/item_attach_photo__ib_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="@dimen/dp_8"
            android:layout_marginEnd="@dimen/dp_8"
            android:src="@drawable/ic_delete_circle_24" />

        <ImageView
            android:id="@+id/item_attach_photo__iv_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center"
            android:src="@drawable/ic_add_2_24"
            app:tint="@color/black" />
    </FrameLayout>
</com.google.android.material.card.MaterialCardView>